def subSequence(s,t):
    substring=True
    for char in s:
        if char not in t:
            substring=False
            break
    if substring:
        return True
    else:
        return False
s=input('Enter the string s : ')
t=input('Enter string t :')
print(subSequence(s,t))